# /review performance-issues

Use the repository code-review Skill and run `node cursor-skills/code-review/scripts/review-changed-files.mjs --scope performance-issues --format markdown`. Focus on effect dependencies, expensive render work, and dependency risk.
