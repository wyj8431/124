# /review changed-files

Use the repository code-review Skill and run `node cursor-skills/code-review/scripts/review-changed-files.mjs --scope changed-files --format markdown`. Review the current staged, unstaged, and untracked files with cross-file context.
