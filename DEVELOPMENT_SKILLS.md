# 灵图工坊开发 Skills 提交

> 更新日期：2026-08-22
> 适用项目：React 19 + Vite + Tailwind CSS 前端，Spring Boot 3.2 + Java 21 后端。

## 一、当前开发进度

| 模块 | 当前状态 | 依据 |
| --- | --- | --- |
| 首页与导航 | 已完成核心页面、侧边栏、搜索、模板瀑布流和热点日历 | `frontend/src/pages/HomePage.tsx` 与路由配置 |
| 用户与会员 | 已具备登录、注册、JWT 鉴权、会员及订单相关页面/接口 | `AuthController`、`JwtAuthFilter` 及用户中心路由 |
| 模板与设计 | 已具备模板搜索、按分类筛选、基于模板创建设计的接口 | 前端 API 映射与 `TemplateController`、`DesignController` |
| AI 与专题 | 已具备 AI 工具、AI 海报和 AI 视频专题页面及后端控制器 | `ToolsPage`、专题页面和 `AiGenerateController` |
| 本地联调 | 已验证前端 `http://localhost:5173` 和后端 `GET /admin/home/index` 正常响应 | 2026-08-22 本地运行验证 |
| 品牌 | 首页顶部共用品牌文字已更新为“灵图工坊” | `PlatformBrand.tsx` |
| 前端构建 | `npm run build` 已通过，产物已生成到 `frontend/dist` | 2026-08-22 本地构建验证 |

### 当前待处理项

1. 设计编辑器的画布 CRUD、AI 工具完整流程和模板详情仍需要按产品验收补齐。
2. `npm run lint` 当前有 9 条非阻断警告，主要涉及 `useMemo` 依赖和 Context 文件的 Fast Refresh 导出规则。
3. 项目 README 中的后端地址和本机 JDK 路径需要在发布前统一校正；当前本地后端实际运行在 `8081`。

## 二、提交规则

每项需求、修复或联调完成后，至少提交一条对应的开发 Skill 记录。记录必须写清楚：目标、实际改动、验证结果、风险或阻塞项。不要把未验证的结果写成“已完成”。

## 三、可复用开发 Skills

### 1. 每日开发日报

**适用场景：** 每日开发结束、阶段验收前、需要向项目负责人同步进度时。

**提示词：**

```text
请根据今天在“灵图工坊”项目中实际完成的工作生成开发日报。

项目技术栈：React 19 + Vite + Tailwind CSS；Spring Boot 3.2 + Java 21；接口前缀为 /admin。
仅根据已验证的代码、接口响应、构建或测试结果输出，不能猜测。

按以下结构输出：
1. 今日完成：按前端、后端、联调、文档分类列出文件或接口。
2. 验证结果：写明执行的命令、接口或页面，以及结果。
3. 风险/阻塞：列出错误、依赖、待确认事项及影响范围。
4. 明日计划：按优先级给出不超过 3 项可执行任务。

工作记录：
{{粘贴当天的改动、命令输出和验收结果}}
```

### 2. 前端页面与组件实现

**适用场景：** 首页、模板中心、AI 工具、用户中心或会员页面新增/改造。

**提示词：**

```text
在灵图工坊前端实现以下需求：{{需求描述}}。

先检查现有路由、页面、组件、API 类型和样式约定；优先复用 src/components、src/api、src/context 中的已有能力。
保持 React + TypeScript + Tailwind 的现有写法，不引入重复的全局状态或无关依赖。

完成后输出：
1. 修改的文件与原因；
2. 用户操作路径和状态处理（加载、空数据、失败）；
3. 已运行的类型检查、构建或页面验证；
4. 未解决的风险。
```

### 3. 后端接口与数据联调

**适用场景：** 新增 `/admin` 接口、调整实体/DTO、补齐前后端真实数据链路。

**提示词：**

```text
为灵图工坊实现并联调以下接口需求：{{接口需求}}。

后端使用 Spring Boot 3.2、MyBatis-Plus、H2/MySQL 和 JWT；所有业务接口使用 /admin 前缀和统一响应结构。
先确认现有 Controller、Service、Mapper、DTO、权限规则及前端调用位置，再进行最小范围修改。

必须验证：
1. 请求参数和响应字段与前端类型一致；
2. 未登录、参数错误、空数据和成功路径；
3. 本地接口响应；
4. 数据库变更或兼容性风险。
```

### 4. UI 还原与视觉验收

**适用场景：** 按截图、设计稿或现有页面还原界面；修改品牌、布局、字体、颜色或响应式样式。

**提示词：**

```text
根据提供的参考图调整灵图工坊页面：{{页面和目标描述}}。

先定位实际渲染该区域的组件，避免只改相似但未使用的代码。复用现有设计 token、Lucide 图标和 Tailwind 样式。
在桌面与移动端检查文字不溢出、组件不重叠、交互状态完整；不要修改 API、数据模型或不相关页面。

完成后说明定位到的组件、视觉改动、截图/浏览器验证结果和剩余差异。
```

### 5. 构建失败与类型问题排查

**适用场景：** `npm run build`、Vite、Spring Boot 启动或接口运行时报错。

**提示词：**

```text
排查灵图工坊的以下错误：{{完整错误日志}}。

先复现并定位到文件、行号和根因；区分当前改动引入的问题与项目已有问题。
只修复与目标错误直接相关的代码，保留现有业务行为和 API 契约。

输出格式：根因、修复方案、修改文件、复现/验证命令、结果、遗留错误。
```

### 6. 发布前回归检查

**适用场景：** 合并前、演示前、交付前。

**提示词：**

```text
对灵图工坊执行一次发布前回归检查。

覆盖以下主路径：首页聚合数据、模板搜索、登录/注册、基于模板创建设计、AI 工具入口、用户中心。
检查前端开发服务、后端 /admin 接口、鉴权边界、错误提示和构建结果。

按“通过 / 失败 / 未执行”列出每项检查，并为失败项给出可定位的文件、接口或命令；不得把未执行项目标记为通过。
```

## 四、日报记录模板

```md
## YYYY-MM-DD 开发日报

### 今日完成
- 前端：
- 后端：
- 联调/文档：

### 验证结果
- 命令或接口：
- 结果：

### 风险与阻塞
- 问题：
- 影响：
- 所需支持：

### 下一步
1.
2.
3.
```

## 五、本地验证基线

```powershell
# 后端（本机 Java 21 路径）
$env:JAVA_HOME = 'D:\Program Files\Java\jdk-21.0.12'
cd backend
.\mvnw.cmd spring-boot:run

# 前端
cd frontend
npm run dev

# 首页聚合接口
Invoke-WebRequest http://localhost:8081/admin/home/index -UseBasicParsing
```

前端默认地址为 `http://localhost:5173`，后端默认地址为 `http://localhost:8081`。若环境不同，应在日报中记录实际端口和差异原因。
