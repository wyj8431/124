# /review changed-files

Review the current staged, unstaged, and untracked files with the canonical repository Skill. Run:

```powershell
node cursor-skills/code-review/scripts/review-changed-files.mjs --scope changed-files --format markdown
```

Inspect cross-file imports and consumers before reporting. Return the Skill's standard structured findings.
