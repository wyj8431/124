# /review component-standards

Review the current frontend change set for component naming, props/state boundaries, and accessible controls:

```powershell
node cursor-skills/code-review/scripts/review-changed-files.mjs --scope component-standards --format markdown
```

Keep only actionable findings with file and line locations.
